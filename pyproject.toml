[project]
name = "pytorch-training-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "eswarashish", email = "ashish.kusumanchi@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
pytorch-training-api = "pytorch_training_api:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
